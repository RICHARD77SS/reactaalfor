import React from 'react';

import { FilterTitleStyle } from './style';

export const Title = (
  {
    title,
  }
) => {
  return (
    <FilterTitleStyle> 
      {title}
    </FilterTitleStyle>
  );
};

