import styled from "styled-components";

export const FoldersStyle = styled.div`
  width: 45%;

  height: auto;
  margin: 0 1rem;
  background: ${props => props.theme.colors.background};
  display: flex;
  align-items: center;
  justify-content: center;
`
