import styled from 'styled-components';

export const MenuIcon = styled.div`
  margin-top: 1rem;
  margin-left: 10px;
  position: relative;
  height: auto;
  width: fit-content;
  left: 1%;
  cursor: pointer;

`;

